.. _dijit/form/_FormValueMixin:

==========================
dijit.form._FormValueMixin
==========================

:Authors: Doug Hays
:since: 1.7

Mixin for widgets corresponding to native HTML elements such as `<input>` or `<select>`
that have user changeable values.